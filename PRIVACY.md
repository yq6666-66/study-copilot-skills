# 隐私说明

学习副驾 Study Copilot 处理的数据全部由用户掌控。发布者不接收任何笔记、错题、真题或学习记录。

## 本地数据

| 数据 | 位置 | 说明 |
| --- | --- | --- |
| 配置 | 用户数据目录（`STUDY_COPILOT_HOME` → `~/.study-copilot/` → 旧版 `~/.codex/kaoyan-408/`） | Vault 路径与开关 |
| 错题/档案/进度 | 用户自己的 Obsidian Vault | 便携学习记录 Schema，用户随时可删 |
| 端侧检索索引 | Vault 内 `.index/` 目录 | 本地向量索引，不上传 |
| 端侧模型 | 仓库 `models/` 缓存 | bge-small-zh ONNX，首次运行下载 |

## 云端 Qwen 调用

- 触发范围：仅「错题陪练」链路（`kaoyan-qwen-drill`），经 DashScope 官方接口发送。
- 发送内容：**只有学科文本**——知识点、错因描述、出题/讲解要求。
- 不发送：用户名、笔记全文、Vault 路径、凭据、身份信息。
- 调用留痕：本地 `logs/qwen/*.json`（消息、回复、token 用量、时延），不含 API Key（引擎内置断言）。日志仅供用户自查与 AI 实践佐证，可随时删除。

## 开发辅助披露

本项目开发过程使用大模型辅助编程（GLM 与阿里云 Qwen——后者生成的法考配置包、错因聚类报告与文档 FAQ 已随仓库入库，调用留痕见 `logs/qwen/`）；运行时云端推理通道为 DashScope Qwen 与宿主 Agent 自身模型。开发辅助不构成对用户数据的数据处理。

## 无遥测

本项目无任何统计、崩溃上报或遥测代码。安装器只把文件复制到用户本机。

## 联系

见仓库 README 中的项目主页。
