# 双模型同题对比（格林公式方向错因 · 真实调用）

> 同一 prompt 分别调用 qwen-flash 与 qwen3.8-flash（DashScope），留痕：`logs/qwen/20260913-235032-qwen-flash.json`、`logs/qwen/20260913-235047-qwen3.8-flash.json`。

| 维度 | qwen-flash | qwen3.8-flash |
|---|---|---|
| tokens | 2440 | 1289 |

## qwen-flash 输出（节选）

```
**题目：**  
设 $ D $ 为平面区域 $ x^2 + y^2 \leq 1 $，$ C $ 为 $ D $ 的正向边界曲线（逆时针方向），函数 $ P(x, y) = -y + x^2 $，$ Q(x, y) = x + y^2 $。计算曲线积分  
$$
\oint_C (P\,dx + Q\,dy)
$$  
并判断下列选项中哪一项正确：

A. 积分值为 $ 2\pi $，且使用格林公式时应取顺时针方向  
B. 积分值为 $ \pi $，且使用格林公式时边界方向应为逆时针  
C. 积分值为 $ 2\pi $，且使用格林公式时边界方向应为顺时针  
D. 积分值为 $ \pi $，且使用格林公式时边界方向应为逆时针  

---

**答案：**  
**B**

---

**三步解析：**

**第一步：确认格林公式的适用条件**  
给定 $ P(x,y) = -y + x^2 $，$ Q(x,y) = x + y^2 $，二者在闭区域 $ D: x^2 + y^2 \leq 1 $ 内连续可导，满足格林公式应用条件。

格林公式为：
$$
\oint_C P\,dx + Q\,dy = \iint_D \left( \frac{\partial Q}{\partial x} - \frac{\partial P}{\partial y} \right) d
```

## qwen3.8-flash 输出（节选）

```
**原创变式题**

设 \(L\) 为椭圆
\[
\frac{x^2}{4}+y^2=1
\]
的边界，方向为**顺时针**一周。计算曲线积分
\[
I=\oint_L \left(y\,dx+x^2\,dy\right).
\]

**选项：**

A. \(-2\pi\)

B. \(0\)

C. \(2\pi\)

D. \(4\pi\)

---

**答案：** C

---

**三步解析：**

1. **判断方向：**  
   格林公式默认适用于边界曲线的正向，即区域始终在行进方向左侧的方向。对于平面闭曲线，正向通常为**逆时针**。本题中 \(L\) 为**顺时针**，属于负向，因此使用格林公式时需要加负号。

2. **应用格林公式：**  
   设
   \[
   P(x,y)=y,\qquad Q(x,y)=x^2.
   \]
   若 \(L\) 为正向，则
   \[
   \oint_L P\,dx+Q\,dy=\iint_D\left(\frac{\partial Q}{\partial x}-\frac{\partial P}{\partial y}\right)dxdy.
   \]
   本题 \(L\) 为顺时针，所以
   \[
   I=-\iint_D\left(\frac{\partial Q}{\partial x}-\
```

> 两份完整输出以留痕 JSON 为准；本项目按需选型（日常陪练用 flash 控成本，复杂精讲可升级）。
