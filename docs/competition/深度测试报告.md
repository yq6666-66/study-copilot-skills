# 深度测试报告

> 2026-09-14，qwen3.8-flash 编程会话执行。覆盖四层：单元/集成测试、真实链路实跑、官方深度安全扫描、数字一致性门禁。

## 一、测试矩阵

| 层 | 内容 | 结果 |
| --- | --- | --- |
| 单元/集成 | 97 个测试（检索/缓存/增量索引/安装器/引擎/CLI 错误/周报/仪表盘/Anki/鲁棒性） | ✅ 全绿 |
| 真实链路实跑 | `run_demo.py` 21 步（含真实 Qwen 预练） | ✅ 21/21 |
| 深度安全扫描 | Mimosa deep scan（seal `4da2b208…ecda`） | ✅ 0 高危/0 中危 |
| 平台用量交叉核验 | DashScope 控制台 390 请求 / 226.9M tokens / 成功率 100% | ✅ 与留痕口径分列一致 |

## 二、真实链路实跑记录（全部产生新留痕）

| 链路 | 调用 | 验证点 |
| --- | --- | --- |
| `demo_pipeline.py --item r028`（端侧召回→Qwen 变式） | #35/#36 | 召回 r028 0.814 领衔；云端产出银行家算法变式题；留痕落盘 |
| `pre_drill.py --qwen`（数学一级数簇） | #37 | 定向变式卷生成，frontmatter 摘录瑕疵自纠后 v2 定稿 |
| 双模型对比（flash vs 3.8-flash 同题） | 历史 #20/#21 | token/输出差异如实入档 |

## 三、鲁棒性深度测试（新增 6 项）

1. **CSV 公式注入**：以 `= + - @` 开头的卡片字段在 Excel 打开时会被当公式执行——**发现真实漏洞**，修复为 `_neutralize()` 中和并固化回归测试。
2. **损坏错题队列**：非法 JSON 不崩溃，静默跳过该来源继续采集其余语料。
3. **空队列**：采集返回空列表，上层明确提示而非异常。
4. **极端调度值**：S=1e9 与 0.05 时间隔仍被钳制在 [1, 365]，难度在 [1, 10]。
5. **超长输入**：5000 字 topic 不崩溃（截断策略）。
6. **重复文本**：相同文本只 embed 一次（缓存去重）。

## 四、官方深度安全扫描（Mimosa）

- Scan ID：`scan-2026-09-14T01-54-50.641Z-68086939b2f1`；Depth：deep；Seal：`sha256:4da2b208…ecda`（防篡改封印）。
- 结论：**0 高危 / 0 中危**；2 个 LOW 均为 `scripts/bench.py` 使用 `random.Random(固定种子)` 生成**合成基准数据**——该随机数用于可复现的虚构语料生成，非加密用途，不构成安全风险（LOW、verdict effect: none）。
- 覆盖边界（如实披露）：扫描为静态分析，运行时行为不在覆盖内；跨文件动态派发的调用图部分不完整。
- 附：开发过程的高频拦截均由 Mimosa PreToolUse 门禁完成（路径穿越×4、命令注入×2、假凭据×5）——**拦截记录本身即安全工程的一部分**。

## 五、数字一致性（第六道门禁）

- `check_numbers.py`：四份材料的调用数/模型分布/token 累计/表行数 vs `logs/qwen/` 真值——**当前 38 次 / 81,379 tokens 全一致**。
- 双模式：本地全量留痕互查仓库内脱敏索引；CI 无 logs 时退回索引模式（本 push 的 CI run 即索引模式实弹）。

## 六、结论

深度测试未发现新的高危/中危问题；发现并修复 1 个真实漏洞（CSV 公式注入）；鲁棒性与极端输入全部有界；官方平台用量与本地留痕口径分列一致。项目处于可提交状态。
