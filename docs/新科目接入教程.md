---
generator: Qwen (qwen-flash)
log: logs/qwen/20260913-232648-qwen-flash.json
---

# 新科目接入实操教程

tags: [Qwen生成, 教程]

> [Qwen生成] 由云端 Qwen（qwen-flash）真实生成，调用留痕：`logs/qwen/20260913-232648-qwen-flash.json`（1974 tokens）。

# 新科目接入实操教程（三步法）

> 本教程基于真实配置模板（法考、注会）设计，适用于新考试科目快速接入系统。全程无需修改底层代码，仅需三步操作即可完成从“准备材料”到“错题闭环验证”的全流程。

---

## ✅ 第一步：准备材料 —— 输出标准化科目元数据

**目标**：构建符合系统规范的 `subject.json` 配置文件，包含科目基础信息与学习路径。

📌 **具体命令示例**：
```bash
# 使用向导指令生成初始配置骨架
ask assistant "生成一个名为‘教师资格证’的新科目配置，包含笔试和面试两部分，使用 kaoyan-material-study-assistant 作为教练技能"
```

💡 系统将返回如下结构化输出（可直接保存为 `subject.json`）：
```json
{
  "package": "teacher_cert",
  "displayName": "教师资格证",
  "description": "全国中小学教师资格考试，评估申请者是否具备从事教育教学工作的基本素质和能力。",
  "examMeta": {
    "schedule": "笔试通常在每年3月和9月举行，面试在次月进行，具体时间以教育部考试中心公告为准。",
    "source": "https://ntce.neea.edu.cn"
  },
  "subjects": [
    {
      "id": "written",
      "name": "笔试",
      "coachSkill": "kaoyan-material-study-assistant",
      "syllabusTopics": [
        "教育知识与能力",
        "综合素质",
        "学科知识与教学能力"
      ]
    },
    {
      "id": "interview",
      "name": "面试",
      "coachSkill": "kaoyan-material-study-assistant",
      "syllabusTopics": [
        "结构化问答",
        "试讲",
        "答辩"
      ]
    }
  ]
}
```

✅ 操作提示：
- `package` 字段建议使用小写英文缩写，如 `teacher_cert`。
- `coachSkill` 固定为 `kaoyan-material-study-assistant`，表示复用已有学习辅导逻辑。
- 所有内容必须来自官方权威来源，避免主观描述。

---

## ✅ 第二步：由向导 Skill 生成 profile —— 自动填充学习资源与题库结构

**目标**：通过 AI 向导自动解析大纲、生成知识点树形结构，并绑定题库入口。

📌 **具体对话示例**：
```text
请根据以下科目配置，自动生成完整的学习 profile：
{
  "package": "teacher_cert",
  "subjects": [
    {
      "id": "written",
      "name": "笔试",
      "syllabusTopics": [
        "教育知识与能力",
        "综合素质",
        "学科知识与教学能力"
      ]
    }
  ]
}
```

⚡ 系统响应将包括：
- 每个主题下的子知识点（如“教育知识与能力” → “教育学原理”、“心理学基础”等）
- 对应的真题标签（如 `#2024_3_笔试`）
- 推荐刷题顺序与难度分级
- 可调用的练习模式（章节练、模拟卷、错题重做）

✅ 输出结果示例（片段）：
```json
"profile": {
  "subjectId": "written",
  "knowledgeTree": [
    {
      "topic": "教育学原理",
      "subTopics": ["教育的本质", "教育目的", "教育制度"],
      "resources": [
        "https://ntce.neea.edu.cn/2024/03/15/123456.html",
        "历年真题集第12辑"
      ],
      "practiceModes": ["章节练习", "错题强化"]
    }
  ],
  "autoTaggingRules": {
    "pattern": "教师资格证_\\d{4}_\\d{1,2}_笔试",
    "label": "teacher_cert_written"
  }
}
```

📌 此步骤完成后，该科目已具备完整学习路径，可立即投入使用。

---

## ✅ 第三步：验证错题闭环 —— 模拟用户答题并追踪反馈链路

**目标**：确认“答题 → 错题记录 → 推送复习 → 再测试”机制正常运行。

📌 **具体命令示例**：
```bash
simulate user "在‘教育知识与能力’模块中完成一次章节练习，故意答错3道题，然后触发‘错题重做’功能"
```

🔍 系统行为验证点：
| 验证项 | 是否通过 |
|--------|----------|
| 用户提交后，系统是否正确标记错题？ | ✅ |
| 是否自动生成专属“错题本”并关联原知识点？ | ✅ |
| 是否推送“本周重点复习”提醒？ | ✅ |
| 是否允许再次练习相同题目并对比正确率？ | ✅ |

✅ 结果说明：若上述四点全部通过，则表示 **错题闭环机制已生效**，无需额外开发。

---

## ❓ 常见问题解答（FAQ）

### Q1：科目 ID 命名建议是什么？  
👉 推荐采用 **小写英文 + 下划线组合**，体现科目核心属性，例如：
- 法考 → `fakao`
- 注会 → `cpa`
- 教师资格证 → `teacher_cert`
- 事业单位 → `public_service`
- 医师资格 → `doctor_exam`

⚠️ 禁止使用空格、特殊符号或大写字母（如 `TeacherCert` 不合规）。

---

### Q2：如何验证机制层无需改码？  
👉 通过 **三步验证法** 即可证明无需修改底层代码：

1. **检查配置文件字段**：确认 `coachSkill` 为 `kaoyan-material-study-assistant`（通用技能）；
2. **执行模拟测试**：使用 `simulate user` 命令触发典型流程（答题→错题→重练）；
3. **查看日志输出**：在系统后台搜索关键词 `profile`, `error_closure`, `revisit`，确认无自定义逻辑调用。

✅ 若所有流程均走通且未报错，说明机制层完全复用现有架构，**无需改码**。

---

🎯 总结：  
只需三步——  
1. 用自然语言生成配置；  
2. 调用向导自动建模；  
3. 模拟测试验证闭环。  
即可安全、高效地接入新科目，实现“零编码、高复用、快上线”。
